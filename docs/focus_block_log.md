# MVP-0 Focus Block Log

## Block 01：Vertical Slice 01

- 原计划：初始化 TypeScript + Vite 项目；显示双方完整度和一张可点击卡牌；点击后更新对手完整度。
- 实际结果：完成。页面从 10 / 10 开始，点击卡牌后变为玩家 10、对手 7；生产构建通过；浏览器控制台无错误；Git 仓库已初始化。
- Blocker：PowerShell 禁止执行 `npm.ps1`；改用同一 Node 安装中的 `npm.cmd`。首次构建发现两个 TypeScript 类型问题，见 BUG-001。应用内浏览器无法访问本机开发端口，改用本机 Chrome 完成交互验收。
- 下一个最小动作：T2，只增加纯游戏状态、固定对手回应和五回合流程，不增加其余卡牌。

### 使用性补充

- 用户反馈无法打开本地页面。根因是本地开发服务器在验收结束后已停止。
- 增加 `open-game.cmd`：双击后启动本地服务器，并自动打开游戏网址。
- 使用时必须保持标题为 `MVP-0 Server` 的命令窗口开启。

## Block 02：固定对手回应与五回合状态机

- 原计划：加入回合状态、固定对手回应、胜负判断和结束后输入锁定；仍只保留一张卡。
- 实际结果：完成。每次未获胜的主张使对手完整度减少 3、玩家完整度减少 2，并进入下一回合；第 4 回合击破对手后不再执行对手回应，卡牌被禁用。
- 自动验证：6 个纯规则测试全部通过，覆盖初始状态、普通回合、胜利优先、玩家失败、第五回合超时和结束后忽略输入；生产构建通过。
- 浏览器验证：10/10 → 8/7 → 6/4 → 4/1 → 4/0，最终显示第 4 回合胜利。
- Blocker：无游戏代码 blocker。Chrome 扩展记录了一条与页面功能无关的消息通道错误，未影响游戏运行。
- 下一个最小动作：T3，将卡牌定义改为数据驱动，加入 6 张卡和每回合随机三选一；不增加重开和正式视觉。

## Block 03：六张数据驱动卡牌与随机三选一

- 原计划：把卡牌定义与规则分离；加入 6 张卡；每回合随机展示 3 张不重复卡牌。
- 实际结果：完成。六张卡集中定义在 `src/game/cards.ts`，游戏状态保存当前手牌和“概念区分”的下一张牌加成。
- 自动验证：13 个规则测试全部通过，覆盖抽牌唯一性、六类卡牌效果、胜负边界、非法输入和结束后输入锁定；生产构建通过。
- 浏览器验证：首回合出现 3 张不重复卡；概念区分使对手 10 → 9，并让下一张有据主张造成 5 点影响，使对手 9 → 4。
- Blocker：无。
- 下一个最小动作：T4，加入重新开始按钮、明确胜负面板和更完整的结算反馈。

## Block 04：重新开始与完整状态反馈

- 原计划：增加重新开始按钮、胜负/进行中状态面板、临时加成提示和结束后交互锁定。
- 实际结果：完成。状态面板显示当前结算，概念区分加成有独立提示，重新开始会重置全部游戏状态并重新抽牌。
- 自动验证：14 个规则测试全部通过；生产构建通过。
- 浏览器验证：先制造第 2 回合、玩家 8、对手 9、下一张 +2 的状态；点击重新开始后恢复第 1 回合、双方 10、无临时加成和新手牌。
- Blocker：无。
- 下一个最小动作：T5，完成边界 QA、正式验收 checklist 和 BUG-001 回归确认。

## Block 05：边界 QA 与回归验收

- 原计划：扩展随机性与状态边界测试，确认真实 Bug 记录，形成正式 QA checklist。
- 实际结果：完成。增加 100 个固定随机种子的模拟对局检查，并将功能、入场测试、scope 和已知限制集中记录。
- 自动验证：规则测试、随机模拟和 production build 全部通过；BUG-001 未复现。
- Blocker：无。Leader 必须亲自完成的 Test 1、Test 2 和 Test 4 保持未通过，未由 Codex 代签。
- 下一个最小动作：T6，准备隔离的第七张卡练习和 Git 回退练习说明，不改变 6 张卡基准版本。

## Block 06：Leader 入场练习准备

- 原计划：准备修改卡牌、增加第七张卡、Git 查看/提交/回退/恢复练习，并保持主分支六卡基准。
- 实际结果：完成练习说明；建立 `exercise/seventh-card-demo` 演示分支验证只改卡牌数据即可加入第七张卡，随后返回 `main`。
- 验收边界：演示分支通过测试只证明架构支持该操作，不代表 Leader 已通过 Test 1、Test 2 或 Test 4。
- 下一个最小动作：T7，建立免费静态部署并取得公开浏览器 URL。

## Block 07：公开部署前置检查

- 原计划：创建免费静态部署，取得公开浏览器 URL，并进行跨设备验证。
- 实际结果：本地测试和 production build 通过；公开部署尚未完成。
- Blocker：电脑未安装 GitHub CLI (`gh`)。GitHub 发布工作流要求 `gh` 已安装并完成登录，缺失时必须停止。当前仓库也尚无远程地址。
- 已否决替代方案：不为使用 Sites 托管而把轻量 Vite 项目迁移成 Cloudflare Worker/vinext 架构，该变化超出 MVP-0 的必要复杂度。
- 下一个最小动作：安装 GitHub CLI，运行 `gh auth login` 完成一次登录，然后由 Codex 创建远程仓库、配置 Pages、推送并验证 URL。
